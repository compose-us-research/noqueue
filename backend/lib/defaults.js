const defaults = {
  config: {
    label: 'Shop',
    maxCustomers: 10
  },
  db: {
    user: 'postgres',
    host: 'localhost',
    database: 'postgres',
    password: null,
    port: null,
  }
}

module.exports = defaults
