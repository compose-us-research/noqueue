const defaults = {
  config: {
    label: 'Shop'
  },
  db: {
    user: 'postgres',
    host: 'localhost',
    database: 'postgres',
    password: null,
    port: null
  }
}

module.exports = defaults
